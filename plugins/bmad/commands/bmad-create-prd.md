---
description: 'Create a PRD from scratch. Use when the user says "lets create a product requirements document" or "I want to create a 
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-create-prd/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
