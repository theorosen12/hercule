---
description: 'Initialize test framework with Playwright or Cypress. Use when the user says "lets setup test framework" or "I want to 
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-testarch-framework/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
