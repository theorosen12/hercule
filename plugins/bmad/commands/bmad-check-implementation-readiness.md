---
description: 'Validate PRD, UX, Architecture and Epics specs are complete. Use when the user says "check implementation readiness".'
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-check-implementation-readiness/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
