---
description: 'Generate end to end automated tests for existing features. Use when the user says "create qa automated tests for [featu
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-qa-generate-e2e-tests/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
