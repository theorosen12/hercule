---
description: 'Create system-level or epic-level test plans. Use when the user says "lets design test plan" or "I want to create test 
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-testarch-test-design/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
