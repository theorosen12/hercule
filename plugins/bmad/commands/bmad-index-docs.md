---
description: 'Generates or updates an index.md to reference all docs in the folder. Use if user requests to create or update an index
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-index-docs/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
