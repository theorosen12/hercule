---
description: 'Generate traceability matrix and quality gate decision. Use when the user says "lets create traceability matrix" or "I 
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-testarch-trace/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
