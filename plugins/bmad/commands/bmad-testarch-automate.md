---
description: 'Expand test automation coverage for codebase. Use when user says "lets expand test coverage" or "I want to automate tes
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-testarch-automate/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
