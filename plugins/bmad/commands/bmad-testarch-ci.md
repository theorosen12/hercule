---
description: 'Scaffold CI/CD quality pipeline with test execution. Use when the user says "lets setup CI pipeline" or "I want to crea
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-testarch-ci/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
