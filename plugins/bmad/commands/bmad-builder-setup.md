---
description: Sets up BMad Builder module in a project. Use when the user requests to 'install bmb module', 'configure bmad builder', 
argument-hint: [optional context or input]
allowed-tools: Read
---

Read and follow the instructions in @${CLAUDE_PLUGIN_ROOT}/skills/bmad-builder-setup/SKILL.md.

Task: $ARGUMENTS

If no input is provided, ask the user what they need before starting.
