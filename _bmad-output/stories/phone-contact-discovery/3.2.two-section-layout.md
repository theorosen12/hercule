# Story 3.2: Two-Section Layout for Friend Discovery

## Status

Draft

## Story

**As a** new user on the Friend Discovery screen,
**I want** to see two distinct sections: friends on Nara and contacts to invite,
**So that** I can both add existing users and invite new ones.

## Acceptance Criteria

1. Split content area into two scrollable sections
2. Section 1: "Amis sur Nara" header with suggestions list
3. Section 2: "Inviter des contacts" header with device contacts list
4. Both sections visible simultaneously (vertical scroll)
5. Section 2 hidden if no device contacts available
6. Section 1 shows empty state message if no suggestions found

## Tasks / Subtasks

- [ ] **Task 1: Create Section Header Component** (AC: 2, 3)
  - [ ] Create `SectionHeader` component with title styling
  - [ ] Support optional count badge (e.g., "Amis sur Nara (7)")
  - [ ] Match existing design system typography

- [ ] **Task 2: Refactor FriendDiscovery Layout** (AC: 1, 4)
  - [ ] Replace single content area with ScrollView containing both sections
  - [ ] Add Section 1: "Amis sur Nara" container
  - [ ] Add Section 2: "Inviter des contacts" container
  - [ ] Ensure smooth vertical scrolling between sections

- [ ] **Task 3: Implement Conditional Rendering** (AC: 5, 6)
  - [ ] Hide "Inviter des contacts" section when `deviceContacts.length === 0`
  - [ ] Show empty state in Section 1 when `suggestions.length === 0`
  - [ ] Handle loading state for both sections

- [ ] **Task 4: Update Styles** (AC: 1, 2, 3)
  - [ ] Create section container styles
  - [ ] Add spacing between sections
  - [ ] Style section headers

## Dev Notes

### New Layout Structure

```typescript
// FriendDiscovery.container.tsx
return (
  <S.Container>
    <S.Header>
      <S.HeaderSpacer />
      <S.CloseButtonContainer>
        <IconButton Icon={Cross} variant="ghost" onPress={handleClose} />
      </S.CloseButtonContainer>
    </S.Header>

    <S.TitleContainer>
      {/* Emoji and title based on state */}
    </S.TitleContainer>

    <S.ScrollContainer>
      {/* Section 1: Amis sur Nara */}
      <S.Section>
        <SectionHeader title="Amis sur Nara" count={suggestions.length} />
        {suggestions.length > 0 ? (
          <SuggestionsList ... />
        ) : (
          <EmptyStateMessage message="Aucun ami trouvé sur Nara" />
        )}
      </S.Section>

      {/* Section 2: Inviter des contacts */}
      {deviceContacts.length > 0 && (
        <S.Section>
          <SectionHeader title="Inviter des contacts" count={deviceContacts.length} />
          <InviteContactsList ... />
        </S.Section>
      )}
    </S.ScrollContainer>

    <PhoneEntryBottomSheet ... />
  </S.Container>
);
```

### New Styled Components

```typescript
// FriendDiscovery.styles.ts

export const ScrollContainer = styled.ScrollView(({ theme }) => ({
  flex: 1,
}));

export const Section = styled.View(({ theme }) => ({
  paddingHorizontal: theme.spaces[200],
  marginBottom: theme.spaces[300],
}));

export const SectionHeaderContainer = styled.View(({ theme }) => ({
  flexDirection: 'row',
  alignItems: 'center',
  marginBottom: theme.spaces[150],
}));

export const SectionTitle = styled.Text(({ theme }) => ({
  fontSize: 14,
  fontWeight: '600',
  color: theme.colors.foreground.secondary.default,
  textTransform: 'uppercase',
  letterSpacing: 0.5,
}));
```

### Component: SectionHeader

```typescript
// components/SectionHeader/SectionHeader.component.tsx
interface SectionHeaderProps {
  title: string;
  count?: number;
}

export const SectionHeader: React.FC<SectionHeaderProps> = ({ title, count }) => (
  <S.SectionHeaderContainer>
    <S.SectionTitle>{title}</S.SectionTitle>
    {count !== undefined && count > 0 && (
      <S.CountBadge>({count})</S.CountBadge>
    )}
  </S.SectionHeaderContainer>
);
```

### Files to Create

- `src/domains/auth/register/view/components/SectionHeader/SectionHeader.component.tsx`
- `src/domains/auth/register/view/components/SectionHeader/SectionHeader.styles.ts`
- `src/domains/auth/register/view/components/SectionHeader/index.ts`

### Files to Modify

- `src/domains/auth/register/view/containers/FriendDiscovery/FriendDiscovery.container.tsx`
- `src/domains/auth/register/view/containers/FriendDiscovery/FriendDiscovery.styles.ts`

## Dependencies

- Story 2.3 (Contact Permission and Suggestions Display) - existing implementation to modify

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-02-01 | 1.0 | Initial story creation | BMad Orchestrator |

## Dev Agent Record

### Agent Model Used

*To be filled by dev agent*

### Debug Log References

N/A

### Completion Notes List

*To be filled by dev agent*

### File List

*To be filled by dev agent*

## QA Results

*To be filled by QA agent*
