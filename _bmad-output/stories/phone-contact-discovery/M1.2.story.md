# Story M1.2: Phone Number Entry State

## Status: Done

## Story

**As a** user without a registered phone number,
**I want** to see a prompt to add my phone number in the suggestion module,
**so that** I can enable friend discovery.

## Acceptance Criteria

1. If user has no phone number, display existing phone input component within module
2. Reuse existing `PhoneNumberCard` or equivalent component
3. After successful phone number submission, automatically trigger contact permission request
4. Show appropriate loading state during phone number submission
5. Handle phone validation errors from backend

## Technical Notes

- **Reuse existing component:** `src/domains/user/shared/view/components/PhoneNumberCard/`
- **Mutation:** Reuse existing `setMyPhoneNumber` mutation
- **Flow:** Phone saved → Auto-trigger M1.3 (contact permission)

## User Flow

```
┌────────────────────────────────────────────────┐
│  FriendSuggestionModule                        │
├────────────────────────────────────────────────┤
│                                                │
│  📱 Trouvez vos amis !                    [X]  │
│                                                │
│  Ajoutez votre numéro pour découvrir qui      │
│  de vos contacts est déjà sur l'app.          │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │  +33 6 XX XX XX XX                       │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  [ Ajouter mon numéro ]                       │
│                                                │
└────────────────────────────────────────────────┘
```

## Integration

```typescript
// In FriendSuggestionModule.container.tsx
const { data: user } = useGetMyProfileQuery();

if (!user?.phoneNumber) {
  return <PhoneNumberEntryState onPhoneSubmitted={handlePhoneSubmitted} />;
}

const handlePhoneSubmitted = async () => {
  // Trigger contact permission request (M1.3)
  await requestContactPermission();
};
```

## Error Handling

| Error Code | User Message |
|------------|--------------|
| `INVALID_PHONE_NUMBER` | "Numéro de téléphone invalide" |
| `PHONE_NUMBER_IN_USE` | "Ce numéro est déjà associé à un autre compte" |
| `RATE_LIMIT_EXCEEDED` | "Trop de tentatives, réessayez plus tard" |
| Network error | "Erreur de connexion, réessayez" |

## Dependencies

- **Blocked by:** M1.1
- **Blocks:** M1.3

## References

- Existing component: `src/domains/user/shared/view/components/PhoneNumberCard/`
- Existing mutation: `src/domains/user/profile/infra/mutations/setMyPhoneNumber/`
- PRD: `docs/prd.md` (FR15)

## Change Log

| Date | Version | Description |
|------|---------|-------------|
| 2026-01-26 | 1.1 | Added `PHONE_NUMBER_IN_USE` error handling (aligned with backend 1.2) |
| 2026-01-25 | 1.0 | Initial story creation |
