# Story M1.1: Friend Suggestion Module Container

## Status: Done

## Story

**As a** user viewing my friends list,
**I want** to see a friend suggestion module at the top of the list,
**so that** I can discover friends I might have missed.

## Acceptance Criteria

1. Create `FriendSuggestionModule` component in `src/domains/user/contactDiscovery/view/components/`
2. Integrate module at the top of Friends list in Profile tab
3. Check for `hide_friend_suggestion_profile` flag in AsyncStorage on mount
4. If flag is true, do not render the module
5. Query user profile to check if phone number exists
6. Render appropriate state based on phone number presence
7. Follow container/component pattern with `FriendSuggestionModule.container.tsx`
8. Use Emotion styling matching existing design system

## Technical Notes

- **Location:** Insert in Friends list component (find existing list component in Profile tab)
- **AsyncStorage key:** `hide_friend_suggestion_profile`
- **Pattern:** Follow container/component separation
- **Styling:** Use Emotion (`@emotion/native`) with theme tokens

## Component Structure

```
src/domains/user/contactDiscovery/
├── view/
│   ├── components/
│   │   └── FriendSuggestionModule/
│   │       ├── FriendSuggestionModule.component.tsx
│   │       ├── FriendSuggestionModule.container.tsx
│   │       ├── FriendSuggestionModule.styles.ts
│   │       ├── FriendSuggestionModule.types.ts
│   │       ├── FriendSuggestionModule.messages.ts
│   │       └── index.ts
│   └── containers/
└── infra/
    ├── queries/
    └── mutations/
```

## Module States

| State | Condition | Renders |
|-------|-----------|---------|
| Hidden | `hide_friend_suggestion_profile === true` | Nothing |
| Phone Input | No phone number | Story M1.2 content |
| Loading | Syncing contacts | Loading skeleton |
| Suggestions | Has suggestions | Story M1.4 carousel |
| No Suggestions | No matches found | Story M1.6 content |

## Dependencies

- **Blocked by:** None (can start immediately)
- **Blocks:** M1.2, M1.7

## References

- Existing patterns: `src/domains/user/shared/view/components/ContactSyncCard/`
- Design system: `@nara/evanescent`
- PRD: `docs/prd.md` (FR14, FR25, FR26)
