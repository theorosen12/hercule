# S-01 — Prisma Schema Migration

**Epic:** narative-subscription
**Status:** Done
**Priority:** High
**Layer:** Backend
**Depends on:** none

---

## Goal

Add `subscriptionsEnabled Boolean @default(true)` to the `Narative` Prisma model and generate + apply the additive migration.

---

## Architectural Context [EMBED]

### Decision Log

| # | Decision |
|---|----------|
| D2 | **`subscriptionsEnabled Boolean @default(true)`** on `Narative` — Toggle state stored server-side on the entity it governs. Default `true`: all new naratives are subscription-enabled without extra migration. Separate `SubscriptionSettings` table rejected as over-engineering. |

### Data Model

```prisma
model Narative {
  // ... all existing fields unchanged ...
  subscriptionsEnabled Boolean @default(true)   // NEW — added for subscription toggle

  // existing relations — unchanged
  likedBy              User[]  @relation("LikedNaratives")
  memberList           User[]  @relation("NarativeToUser")
  // ... rest unchanged
}
```

### Migration Plan

- **Migration name:** `{timestamp}_add_subscriptions_enabled_to_narative`
- **Additive-only:** Yes — `Boolean @default(true)` backfills all existing naratives with `true` automatically
- **Backfill required:** No — Prisma default handles it in migration SQL
- **Breaking change to existing API:** No

---

## Files to Create

None — migration file is auto-generated by Prisma CLI.

## Files to Modify

| File Path | Change | Risk |
|-----------|--------|------|
| `backend/prisma/schema.prisma` | Add `subscriptionsEnabled Boolean @default(true)` to `Narative` model | Low — additive |

---

## Tasks

- [x] Open `backend/prisma/schema.prisma` and locate the `Narative` model
- [x] Add `subscriptionsEnabled Boolean @default(true)` to the `Narative` model exactly as defined in §Data Model above — place after the last scalar field, before the relations block
- [x] Run `npx prisma migrate dev --name add_subscriptions_enabled_to_narative` from `backend/` to generate and apply the migration
- [x] Verify the generated SQL contains `ALTER TABLE "Narative" ADD COLUMN "subscriptionsEnabled" BOOLEAN NOT NULL DEFAULT true` (no DROP, no data loss)
- [x] Run `npx prisma generate` to regenerate the Prisma client with the new field

---

## Acceptance Criteria

- [x] `Narative` model in `schema.prisma` contains `subscriptionsEnabled Boolean @default(true)`
- [x] Migration SQL is additive-only (no DROP or destructive statement)
- [x] All existing naratives have `subscriptionsEnabled = true` after migration
- [x] `npx prisma generate` completes without errors
- [x] No regression on existing `Narative` queries

---

## Edge Cases & Required Handling

| Edge Case | Handling Strategy | User-Facing |
|-----------|-------------------|-------------|
| Migration run twice | Prisma migration system is idempotent — already-applied migrations are skipped | No visible effect |

---

## Architectural Constraints

- Do NOT add any field beyond `subscriptionsEnabled Boolean @default(true)` to `Narative`
- Do NOT modify any existing field or relation on `Narative`
- Do NOT create a separate `SubscriptionSettings` table (D2 explicitly rejects this)
- Migration must be additive-only

---

## Dev Notes

- `subscriptionsEnabled Boolean @default(true)` means no backfill script needed — Prisma generates `DEFAULT true` which applies to all existing rows automatically
- `PRISMA_INCLUDE.Narative` in `backend/src/prisma/prisma.include.ts` does NOT need updating — scalar fields are always included without explicit `include`
- After this story, `NarativeEntity` will have `subscriptionsEnabled: boolean` — S-02 and S-03 depend on this

---

## Completion Notes

**Files created:** `backend/prisma/migrations/20260403180300_subscriptions_enabled/migration.sql`
**Files modified:** `backend/prisma/schema.prisma`
**Deviations:** Migration was already applied to the DB from a prior session (migration ID `20260403180300_subscriptions_enabled`). Created the missing migration file manually and reconciled Prisma history — no data loss.
**Escalations:** none
